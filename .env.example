# PostgreSQL Database Config
POSTGRES_USER=homeportal_user
POSTGRES_PASSWORD=homeportal_password
POSTGRES_DB=homeportal_db

# Backend Config (if running locally without Docker)
# DATABASE_URL="postgresql://homeportal_user:homeportal_password@localhost:5432/homeportal_db?schema=public"
# In Docker, DATABASE_URL is constructed automatically in docker-compose.yml

PORT=3000
JWT_SECRET=supersecretjwtkey
